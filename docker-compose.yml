services:
  complete-series-v2:
    build:
      context: .
      args:
        VITE_GOOGLE_BOOKS_API_KEY: ${VITE_GOOGLE_BOOKS_API_KEY:-}
    image: complete-series-v2:local
    ports:
      - "8080:80"
    restart: unless-stopped
